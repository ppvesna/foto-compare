# Ближайший этап

Дата актуализации: 2026-08-17

Ветка: `feature/oleg-work`

## Цель

Продолжить постепенную миграцию к Architecture v2, не ломая рабочий сценарий сравнения.

## Состояние

- Документы Architecture v2 созданы.
- Модуль `lib/features/protocols` выделен.
- Модуль `lib/features/references` выделен и проверен владельцем вручную.
- Публичные входы: `features/protocols/protocols.dart` и
  `features/references/references.dart`.
- Доменные модели протокола отделены от локального `SharedPreferences`-адаптера.
- Старые ключи протоколов, эталонов и JSON-форматы сохранены.
- Добавлены тесты загрузки legacy-протокола и legacy-эталона.
- Добавлены `billing` и `organization`: матрицы Free/Pro/Enterprise, роли, лимиты,
  Supabase-адаптеры и mock-реализации.
- Роли организации упрощены до владельца, администратора, сотрудника и заказчика.
  Менеджер, дизайнер и специалист проверки вынесены в функции конкретной работы.
- Создана доменная основа `features/production`: работа, участник и расчёт доступа к
  работе. Хранение и UI работ ещё не подключены.
- Ключевые этапы сравнения получают снимок прав из `MainShell`.
- Прототип remote-миграции `005_access_control.sql` не применён; его ролевая часть
  требует обновления до проверки на staging.
- Добавлен обратимый smoke-test через защищённый Supabase `app_metadata`; он позволяет
  проверить реальный вход, тариф и роль без применения старой RLS-миграции.
- Добавлен экран «Организация»: создание организации, поиск точного ника, назначение и
  изменение ролей, список участников и ручное обновление серверных прав.
- Миграции `006–008` применены к текущему тестовому Supabase. Проверены серверный Pro,
  создание организации, владелец, один участник и обновление прав. Старая чат-миграция
  `004` для этого не требуется.
- Добавлен `features/chat`: доменные модели, `ChatRepository`, mock и Supabase-адаптер.
  Миграция `016` переиспользует таблицы `004`, удаляет их прототипные общие политики,
  добавляет личные, организационные и job-scoped чаты и Supabase Realtime.
- Миграция `017` добавляет управляемую публикацию конкретной работы заказчику.
  Владелец, администратор или назначенный менеджер открывает и закрывает доступ;
  внутренний чат организации заказчику не показывается.
- В настройках есть раздел «Доступ»: статус, лимиты, capabilities и права роли только из Supabase.
- Добавлен `features/color_analysis`: CIE76, CIE94, CIEDE2000, CMC 2:1 и
  точечная апертура 2/5 мм.
- Добавлен `features/capture`: освещение и оптический фильтр камеры как метаданные съёмки.
- Профиль цветового измерения подключён к сравнению и протоколу.
- Добавлен `capabilities/storage`: контракт `CloudStorage`, mock, локальная конфигурация
  и безопасный режим по умолчанию без загрузки оригиналов.
- В настройках показаны устройство, Supabase Storage, Google Drive и диск организации;
  неподключённые провайдеры честно заблокированы.
- Добавлен раздел «Тариф и оплата»: личная или организационная область, Pro/Enterprise,
  месяц/год, серверная цена, реквизиты для счетов и тестовая активация без списания денег.
- Миграция `019` вводит `current_entitlement_v4`; тариф организации изменяет только владелец.
- Миграция `019` применена к тестовому Supabase. Вручную проверено разделение: личный Pro и рабочий Enterprise организации с отдельным сроком действия.

## Шаг 1. References завершён

- Старый эталон открывается и выбирается активным.
- Создание, удаление и повторная загрузка проверены вручную.
- Тест совместимости legacy-данных проходит.
- Модуль зафиксирован в рабочей ветке.

## Реализованная структура references

Цель: собрать эталоны и калибровочные профили в одном владеющем модуле.

Структура:

```text
lib/features/references/
  domain/
    layout_profile.dart
    saved_reference.dart
  infrastructure/
    reference_storage.dart
    layout_profile_storage.dart
  references.dart
```

Соблюдённые ограничения:

- не менять `reference_profiles_v2` и `reference_profiles_active_id_v2`;
- не менять JSON `LayoutProfile`;
- не менять логику выбора активного эталона;
- не менять `CompareScreen`, кроме путей импорта;
- добавить тест загрузки старого JSON.

## Шаг 2. Модуль production начат

Уже выделено:

- `ProductionJob`;
- стабильный ID и номер работы;
- `JobParticipant`;
- функции менеджера, дизайнера и специалиста проверки;
- правило доступа владельца, администратора, сотрудника и заказчика.

Следующие малые шаги:

1. Добавить `Sample` и номер отпечатка.
2. Добавить локальный репозиторий работ без изменения текущего JSON протокола.
3. Связать выбранную работу, эталон и новый протокол через совместимый адаптер.

На первом шаге данные остаются локальными. Облачная схема не меняется в том же коммите.

## Отдельный rollout прав

1. Не применять старую роль-схему `005_access_control.sql`.
2. Зарегистрировать отдельные тестовые аккаунты сотрудника и заказчика.
3. Проверить поиск по нику, назначение роли и повторный вход каждого аккаунта.
4. Миграция `016` применена: переписка владельца и сотрудника проверена в двух аккаунтах.
5. Применить миграцию `017` на тестовом Supabase.
6. Открыть одну назначенную работу заказчику и проверить ее чат во втором браузере.
7. Закрыть доступ и проверить, что работа сразу исчезла у заказчика.
8. Проверить, что заказчик не видит общий чат организации и чужие работы.
9. Проверить изоляцию работ, протоколов и файлов между двумя заказчиками.
10. Перед production сделать backup и повторить тест-план на staging.

## Шаг 3. Фасад inspection

Только после `protocols`, `references` и `production` создать единый прикладной сценарий:

```text
RunInspection
```

Он будет координировать:

1. подготовку и совмещение;
2. предварительную карту;
3. геометрию;
4. коды;
5. OCR;
6. Lab ID;
7. точную Delta E;
8. сохранение протокола.

`CompareScreen` сначала будет вызывать фасад, а не разбираться на множество экранов и контроллеров.

## Порядок следующих платформенных модулей

1. `CloudStorage`: основа и локальный режим готовы; далее Supabase Storage, затем Google Drive.
2. `DatabaseConnector`: подключение базы заказчика через server-side connector; пароль базы не попадает в web-клиент.
3. `AIProvider`: предварительная оценка и анализ результатов сравнения через заменяемый серверный адаптер.

Функционал сравнения можно отлаживать параллельно, потому что эти модули общаются с ним через контракты.

## Не входит в ближайший этап

- Полное переписывание `CompareScreen`.
- Замена Supabase.
- Разделение на микросервисы.
- Полное облачное хранение изображений.
- Реальный платежный провайдер и Enterprise-лицензии.
- PDF-отчёты.

## Критерий безопасности

Каждый шаг должен:

- сохранять старые локальные данные;
- не менять поведение UI;
- не менять алгоритм вместе с переносом;
- проходить анализ и веб-сборку;
- иметь отдельный понятный diff и коммит.
