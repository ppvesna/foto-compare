# Лог действий (кратко)

- Обновлён README: калибровка + Supabase
- Supabase: прогнана миграция, проверены таблицы и бакет `layouts`
- Fix: StartScreen — обёрнут в Scaffold (ошибка Material)
- Fix: веб — path_provider (kIsWeb-проверки) + неоднородный Border+radius
- Fix: нагрев CPU при сравнении — нормализация яркости на уменьшенных копиях
- Создан CLAUDE.md — правило "Делай"/"Начинай" перед действиями
- Fix: образец теперь тоже проходит perspectiveCorrect (как эталон)
- Добавлено сохранение текстовых маркеров результата в check_results
- Добавлен SESSION_CHANGES.md и PLAN_NEXT_STAGE.md (план переработки)
- Fix: _saveCheckResult пропускается на вебе (sqflite не работает в браузере)
- AnchorPointScreen: добавлен зум/сдвиг вида (кнопки, колесо мыши, пинч)
- Новый экран OverlayAlignScreen: точное совмещение по кнопкам (зум/сдвиг/прозрачность)
- На вкладке Сравнение: кнопка «🎯 Точное совмещение» (Эталон/Образец)
- Калибровка (точки) сделана обязательной: баннер-подсказка, иконка-индикатор,
  «Сравнить ›» заблокирована без _layoutProfile
- _layoutProfile сбрасывается при любом изменении эталона/образца (10 мест)
- Новая вкладка «🎯 Совмещение»: сюда перенесён блок «Совместить с эталоном»
  (наложение, перспектива, точное совмещение, авто-выравнивание, калибровка,
  кнопка «Сравнить ›») — было в конце вкладки «Образец»
- Вкладки теперь: Эталон → Образец → Совмещение → Результат → История
- Убрана «📸 Серия снимков» (усреднение кадров) — телефон делает это сам
  (HDR/ночной режим). Удалены _pickMultipleAndStack, _averageImages.
- Кнопка «🔀 Объединить» (Эталон2/Образец2) переименована в «🔀 Склейка кадров»
- Новый виджет XpCollapsible (спойлер); блоки «Эталон 2» и «Образец 2»
  свёрнуты по умолчанию, разворачиваются по тапу (нужны только для «Склейка кадров»)
